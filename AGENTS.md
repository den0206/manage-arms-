# Agent instructions

このリポジトリの作業ルールの正本は [CLAUDE.md](CLAUDE.md) です。
内容を複製せず、必ず `CLAUDE.md` を読んでから作業してください。

リポジトリ固有のコマンドは `.claude/commands/` にあります。

- `/review-for-merge` — マージ前レビューと完了チェック
- `/commit-by-feature` — レビュー後に機能単位でコミット
